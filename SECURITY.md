# Security

This userscript is designed to run only on ChatGPT domains listed in its metadata and currently requests no additional userscript permissions (`@grant none`). It does not intentionally transmit conversation content or usage data to external services.

For a security-sensitive report, avoid posting private conversation data, tokens, credentials, or other secrets in a public issue. Contact the repository owner through GitHub with a minimal reproducible description first.
